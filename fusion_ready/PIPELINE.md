# PIPELINE — 세그멘테이션에서 프린트·팬텀·CFD 도메인까지 (2026-09-16 기준)

한 케이스(MM-WHS case 1009)에 대해 아래 순서대로 돌리면 모든 산출물이 재현된다. 각 단계는
**어디서 돌아가는지**(샌드박스 Linux / Windows Blender)와 **무엇을 검증하는지**를 적었다.
다른 케이스에 쓰려면 1→13을 그대로, 상수는 각 스크립트 상단에서만 바꾼다.

## 환경

| 환경 | 용도 | 준비 |
|---|---|---|
| Python 3.10 + trimesh 4.12 (샌드박스 또는 아무 Linux/WSL) | 1–4, 6–13 | `pip install --target /tmp/pylibs fast-simplification rtree pymeshfix shapely mapbox-earcut manifold3d scikit-image` 후 `PYTHONPATH=/tmp/pylibs` |
| Blender 5.2 (Windows) | 5만 | `run_blender_pipeline.bat` 또는 Claude Code에서 `blender --background --python blender_pipeline.py` |

샌드박스는 3 GB RAM. **큰 메시(≥40k면)에 `trimesh contains()`/`volume_mesh()`(ray-cast)를
쓰지 말 것** — 두 번 OOM. 대신 `lv_sdf_grid.npz`(단계 4)의 SDF 보간이나 작은 파트에만 `contains()`.

## 단계

| # | 스크립트 | 입력 → 출력 | 검증 / 판정 기록 |
|---|---|---|---|
| 1 | `normalise_assembly.py` | `lv_cfd_anatomical/constant/triSurface/*.stl` (m·mm 혼재, 4개 프레임) → `frame_A_patient/`, `frame_B_papillary/`, `frame_C_stacom_UNREGISTERED/`, `frame_D_orphan/`, `MANIFEST.json` | 단위·프레임 진단; **frame A만 해부학적으로 일관** |
| 2 | `build_fusion_sets.py` | frame A → `PRINT/` (≤40k면, pymeshfix로 수밀 복원), `CAD/` (≤10k면) | `VERIFY.json`: 감면 오차·수밀·컴포넌트 |
| 3 | `refit_valves.py` | `generate_valves.py` 상수 3개를 실측 LV 축·판륜 위치로 교체 → `*_refit_v1.stl`, `valve_refit.json` | 첨판 LV 내부 비율 87.5% (게이트 90% 미달 → v1 병기). Claude Code의 shrink-wrap v2 97.8% 통과 → `mitral_valve.stl` 승격 (`valve_refit_v2.json`) |
| 4 | `build_sdf_grid.py` | `PRINT/frame_A_patient/lv_surface.stl` → `lv_sdf_grid.npz` (2.5 mm, +12 mm 여유) | 이후 모든 포함 검사의 기반 |
| 5 | `blender_pipeline.py` **(Windows)** | frame A LV + 승격 판막 → `BLENDER_OUT/{LV_bloodpool, mitral_valve, aortic_valve}.stl`, `LV_myocardium_frameA.stl`(cardiac_meshes를 X-반사로 frame A에 정합), `frame_transform.json` | `FINDINGS.md`: 판막은 두께 0 시트 → Solidify 1.2 → voxel 0.25; `cardiac_meshes/`는 **좌우 거울상** |
| 6 | `make_av_plate.py` | 심근 기저 단면 + 승모판 시트의 실제 판륜 → `frame_A_patient/av_plane_plate.stl`, `av_plate.json` | 판 12.5 mm(+4.5~−8.0), D자 승모판 구멍(판륜 −1.5 mm 오프셋), 원형 대동맥 구멍; **실제 판륜 87% 매립**(나머지 13% = 대동맥 구멍 구간, 의도) |
| 7 | `build_hollow_v2.py` | 심근 ∪ 판 ∪ 승모판 ∪ 대동맥판 (manifold3d) → `BLENDER_OUT/hollow_ventricle_v2_fixed_valves.stl`, `hollow_v2_check.json` | 751,754면, 수밀, 단일, 163.9 mL; 공동 차단 1.7%, 두 구멍 축 통과 |
| 8 | `extract_pm.py` | 심근 ∩ closing(혈액풀) → `pm_extract.json` | **판정: 유두근이 심근 라벨에 없음**(교집합 = 0.63 mm 필름 하나, closing이 채운 부피 0.4 mL). 환자 유두근 추출 불가 — 팬텀/CFD 공동은 "유두근 포함 공동" |
| 8′ | `pm_register.py` | frame B 유두근의 6-DOF 강체 정련 → `pm_registration.json`, `*_bestfit_NOTACCEPTED.stl` | **강체 정합 불가** — 모든 게이트 근소 동시 위반. 사용 안 함 |
| 9 | `generate_subvalvular.py` | 교과서 해부 위치의 **이상화** 유두근 2개 + 건삭 10개 → `frame_A_patient/{pm_*,chordae}_IDEALISED.stl`, `BLENDER_OUT/hollow_ventricle_v3_IDEALISED_subvalvular.stl`, `subvalvular.json` | 754,854면, 수밀, 단일, +2.9 mL; 건삭 10/10 공동 내 경로; 구멍 축 통과 |
| 10 | `make_cutaway.py v2` / `v3` | 장축+MV–AV 선을 포함하는 면으로 반절 → `BLENDER_OUT/hollow_v*_cutaway_{A,B}[_PRINT_ORIENTED].stl`, `cutaway_v*.json` | 각 반쪽 수밀·단일; 부피 합 = 전체 |
| 11 | `make_flow_phantom_mold.py` | 혈액풀 + 포트(Ø19 / Ø15.8) → `PHANTOM/core_LV_with_ports.stl`, `mould_box.stl`, `*_PRINT_ORIENTED.stl`, `phantom_design.json` | 코어-상자 접촉 0, 실리콘 ≥15 mm, 주입면 열림·포트면 닫힘, 다리 > 스터브 돌출 |
| 12 | `export_cfd_domain.py` | 코어 → `PHANTOM/cfd/LV_phantom_{m,mm}.stl` (`inlet`/`outlet`/`LV` 패치), `cfd_domain.json`, snappyHexMesh 스니펫 | 유입 2.83 cm², 유출 1.96 cm², 재조립 시 열린 모서리 0 |
| 14 | `ct_refine_lv.py` | **원본 CT** `MM-WHS/ct_train/ct_train_1009_{image,label}.nii.gz` → 라벨 500 안에서 Otsu(235 HU) → `CT/lv_bloodpool_CT.stl`(진짜 혈액 132.5 mL), `CT/LV_myocardium_CT_with_PMs.stl`(라벨 205 + 공동 안 근육 17.8 mL), `CT/pm_candidate_*_CT.stl`, `ct_refine.json` | 영상→frame A: **회전 없음, 평행이동 (37.08, −31.48, 159.46), 평균 0.19 mm** (assert < 1 mm). 환자 유두근 2개: 전측 2.1 mL(−121°), 후내측 3.1 mL(+153°) |
| 15 | `ct_prepare_parts.py` | Taubin 10회 + 감면(316k/149k) + 수밀 복원 → `CT/*_smooth.stl`, `CT/blood_sdf_grid.npz`, `CT/pm_tips.json` | 부피 보존 (143.4→142.8, 132.7→132.9 mL) |
| 16 | `build_hollow_v4_CT.py` | CT 심근(유두근·육주 포함) ∪ 판 ∪ 판막 ∪ 진짜 유두근 첨부에서 나가는 건삭 10개 → `BLENDER_OUT/hollow_ventricle_v4_CT.stl`, `hollow_v4_check.json` | 712,322면, 수밀, 단일, 181.8 mL; 차단 2.5%, 구멍 축 통과. `make_cutaway.py v4` → 절개 모형, `hollow_v4_cutaway_render.png` |
| 17 | `export_simvascular.py` | 팬텀 유체 / 라벨-500 공동 / 라벨-205 심근 → `SV/*.vtp` (ModelFaceID, **cm**), `sv_models.json` | 유체 endo 133.6 ≈ 고체 endo 134.3 cm²; 승모판 7.5, 대동맥 3.9 cm². SimVascular 0단계 — 로드맵 `SIMVASCULAR_ROADMAP.md` |
| 18 | `ct_coronary_vesselness.py` → `ct_coronary_seg.py` → `ct_coronary_finalize.py` (QA: `ct_coronary_cpr.py`, `ct_coronary_xsec.py`, `ct_coronary_check.py`) | **원본 CT에서 관상동맥**: 심외막 띠(라벨 밖 ≤14 mm) 안에서 Frangi 관형성(σ 0.5–2.4 mm, 자체 float32 구현, z-슬랩 재개 가능) + HU 이중 문턱(≥180 & V≥0.02 / ≥150 & V≥0.05 / ≥300 두꺼운 덩어리) → 대동맥 기시부(판막 중심 32 mm, 대동맥 라벨 밖 1–4 mm, ≥300 HU) 씨앗에서 측지 재구성(≤170 mm) → 골격 → MST → 가지 그래프 → 잔가지(<3 mm)·정맥(중앙 HU<240, 또는 <330이면서 Ø≥3.2)·심장 밖(>16 mm)·덩어리(Ø≥6) 하위나무 제거 → 방 인접도로 명명 → `CT/coronary/coronary_tree_frameA.stl`(분할면), `coronary_tree_print_frameA.stl`(중심선 튜브, 최소 Ø2 mm, 수밀), `coronary.json`(가지·중심선·반경·trunk 경로) | LM 8.6 / LAD 107 / LCx 55 / RCA 80 / 기타 40 mm 중심선, 1.1 mL. **CPR·단면으로 확인한 신뢰 구간**: LAD 0–35 mm 선명(Ø3), 35–100 mm 가늘게 연속(Ø1.5–2, HU 250–300); LCx ~28 mm; RCA 0–30 mm 선명, **12–23 mm 석회화(700+ HU)**, 그 뒤 우심방 라벨 옆에서 희미 → 마지막 15 mm는 작은 곁가지. 정맥(대심정맥·전실간정맥)은 HU로 제거, 좌심방이 덩어리는 Ø6 규칙으로 제거. RA/RV 라벨이 방실고랑 지방까지 과분할되어 있어 우측은 `HU≥330 & RA/RV 인접` 구제 규칙 사용 |
| 19 | `ct_hires_lv.py` → `build_hollow_v4_CT.py` (`MYO_PATH=CT/hires/LV_myocardium_CT_hires.stl TAG=v5_CT_hires`) → `make_cutaway.py v5` → `render_cutaway_compare.py` | **표면 품질 개선.** 이진 마스크 marching cubes(계단) 대신 **연속 필드** 등위면: L=gauss(라벨 500∪205, σ 1 vox), L500=gauss(라벨 500), Hs=gauss(HU, σ 0.6 vox); F_blood=min(L500−½, (Hs−235)/200), F_myo=min(L−½, −F_blood). 각 등위면의 최대 연결성분만 유지(혈액풀 속 어두운 점 노이즈 = 떠다니는 '근육' 416개, 밝은 점 = 기포 374개 제거) → Taubin 10 → `CT/hires/{LV_myocardium_CT_hires,lv_bloodpool_CT_hires}.stl` → **v5** `BLENDER_OUT/hollow_ventricle_v5_CT_hires.stl` + 절개 A/B | 복셀 스케일 거칠기(RMS, 1 mm 스무딩 대비) 심근 0.190→0.061 mm, 혈액풀 0.234→0.074 mm; 옛 표면→새 표면 평균 거리 0.11/0.16 mm(같은 해부, 표면만 개선); 부피 심근 139.7 mL(v4 부품 142.8), 혈액 135.7 mL. v5: 891,814면, 수밀, 단일, 178.7 mL, 차단 2.4 %, 구멍 축 통과. 그림 `CT/hires/hires_closeup.png`, `BLENDER_OUT/cutaway_v4_vs_v5.png`. 32 GB PC: `TARGET_MYO=1500000 TARGET_BLOOD=800000 TAUBIN=15` |
| 20 | `ct_perfusion_territories.py` (계산 19 s; 그림은 `RENDER_ONLY=1`) | **관류 영역 지도 + AHA 17분절 + 폐색 부위별 위험 심근량.** 심근 복셀(19단계 연속 필드) → 가장 가까운 관상동맥 중심선 점(Voronoi 규칙)으로 LAD/LCx/RCA 배정, 곁가지는 나온 줄기를 상속, LM 제외. 보이는 나무가 근위–중간부뿐이라 **고랑 사전정보**(후실간구 PDA 26점, 하측벽 기저 고리 PLV 12점, 좌방실고랑 원위 LCx 20점, 측벽 OM 선 14점 — 위치는 환자 라벨에서, 공급 줄기만 가정)로 채운 지도(우세 RD/LD 둘 다)와 보이는 나무만 쓴 지도를 함께 냄. AHA 17분절은 MV 중심–심첨 축 + 중격 중심각(RV 라벨 접촉 심근)으로 정의 → `CT/coronary/territories/{territory_report.json, territory_table.md, territory_map.png, LV_myocardium_territories.ply}` | LV 148 g. RD 지도: LAD 38 / LCx 53 / RCA 57 g (보이는 나무만: 97/36/15; LD: 38/108/2 — **우세에 따라 RCA 영역 2–57 g**). AHA 대비 15/17 일치; 보이는 혈관 25 mm 안의 '실측' 분절 5/5 일치, 나머지는 사전정보 기반으로 표시. 위험 심근량: LM 91 g(62 %), 근위 LAD 38 g(26 %), 첫 대각지 이후 중간 LAD 36 g(24 %), 근위 LCx 53 g(36 %), 근위 RCA 55 g(37 %, 전부 사전정보). 한계: 정맥·석회화로 원위부 없음, 관류 측정 아님, 우세 미상 |
| 21 | `case_paths.py` + `ct_case_geometry.py` (`CASE=<id>`) | **N례 일반화.** 케이스 파일 위치·프레임을 한 곳에서 결정: 1009는 기존 레이아웃(frame A) 그대로, 그 외는 `CT/cases/<id>/` + **월드 좌표**. `ct_case_geometry.py`가 라벨만으로 MV 중심(500–420 접촉면 중심), AV 중심(500–820 접촉면), 기저→심첨 축, Otsu, 방별 HU·부피를 `case_geometry.json`에 씀. 모든 CT 스크립트가 `CASE` 환경변수를 읽음. 큰 볼륨(0.35 mm)은 거리장을 2× 다운샘플로 계산(`dist_to`, 4 GB 샌드박스용 안전장치) | 1009: 라벨 기하 vs frame-A 값 — 축 2.3°, MV/AV 중심 10 mm(접촉면 중심 vs 판막 피팅 중심의 정의 차이; 하류 계산에 무해). 관상동맥 명명을 임계값에서 **나무 구조**로 교체(좌/우 계통은 LA→RA 축 투영으로; LM = 첫 실질 분기까지; LAD/LCx는 고랑 점수; RCA 줄기의 후실간구 구간 = PDA; 곁가지 D/OM/RI/PLV/RV-br; 대동맥 테두리 조각 제거). 1009 결과 불변 |
| 22 | `build_whole_heart.py` (`--stage parts\|shell\|union\|render\|all`, 캐시) | **전심장 교육 모형.** 라벨 7개 각각 연속 필드 등위면 → `whole_heart/parts/*.stl`(방 캐스트, 색별 출력); 외피 = LV 심근∪LV 공동∪(RV+3.0 / 심방+2.0 / 대혈관+1.5 mm **합성 벽**) → `whole_heart_shell.stl`; ∪ 관상동맥 튜브(manifold) → `whole_heart_with_coronaries.stl`. 0.35 mm CT는 라벨을 ~0.7 mm로 다운샘플 | 1009: 외피 400k면 수밀 820 mL, 합집합 479k면 수밀 단일, 관상동맥 부피의 89 % 외피 밖(보임). 1001: 93 %. 렌더 `whole_heart_render.png` |
| 23 | `batch_cases.py [--cases …] [--steps …] [--summary-only]` | **N례 배치 실행기.** 케이스마다 geometry→vesselness→seg→finalize→cpr→xsec→hires→territories→whole을 서브프로세스로, 로그 `CT/cases/<id>/logs/`, 실패 시 다음 케이스로; 결과 JSON을 모아 `CT/cases/SUMMARY.md/.csv`, `summary.png` | 1001 전 단계 통과(샌드박스 3 GB에서 케이스당 ~15분; PC1에서는 ~5분 예상). 1001: 대동맥 551 HU, LM 12 / LAD 59 / D 71 / LCx 58 / OM 112 / **RCA 228 (PDA 72)** mm, 영역 LAD 43 / LCx 44 / RCA 64 g, AHA 14/17(실측 10/11), 전심장 수밀 |
| 24 | `ct_great_vessels.py` (`CASE`, `PV_CAP_MM`, `PA_CAP_MM`) → `build_whole_heart.py`가 자동으로 `labels_extended.npz`를 씀 (`whole_heart_ext/`) | **대혈관을 CT에서 직접.** MM-WHS 라벨은 대동맥 상행부·PA 줄기까지만 있고 폐정맥·SVC·IVC는 없음. ~1 mm 작업 격자에서 각 구조를 **자기 라벨에서 자기 HU 창** 안으로 측지 팽창(대동맥 ≥0.8×대동맥 HU, LA/폐정맥 ≥0.85×LA, PA는 [0.72×PA, PA+70], RA ≥ max(150, 0.7×RA)); 서로 닿는 혈관(SVC·우PA·우상폐정맥·상행대동맥)은 **내부거리 워터셰드**로 목에서 먼저 분리하고, 창에서 거절된 밝은 복셀은 2차로 측지 최근접 구조에 배정; RA 연장은 두께 ≥12 mm 관만 → 위치로 SVC/IVC | 1009: 폐정맥 21 mL(5 성분, 346 HU), PA 가지 +16 mL(221 HU, 폐문까지), **SVC 22 mL(422 HU, 우측 주입 농축 조영)**, 대동맥 연장 0.7 mL(**궁은 스캔 FOV 밖**), **IVC 0 — 동맥기라 조영 안 됨(예상된 한계)**. `great_vessels_check.png`(MIP 위 겹침)로 확인. 전심장 ext: 외피 244k면·883 mL 수밀, 합집합 수밀 단일, 관상동맥 85 % 외부. 완전판(IVC 포함, 위상 무관)은 PC1에서 TotalSegmentator 라벨을 넣는 것 |
| 25 | `build_whole_heart_hollow.py` (`--stage hollow\|union\|cut\|render\|all`, 캐시) | **속 빈 전심장 + 4방 절개.** 복셀(확장 라벨 ~1 mm): 외피(합성 벽 RV 3.0 / 심방 2.5 / 혈관 2.0 mm) − 내강(RV·LA·RA·대동맥·PA·폐정맥·SVC) − LV 영역(1 mm 침식 → 1 mm 심외막 스킨이 v5와 겹쳐 이음매 없음) → σ 0.7 등위면. 메시: ∪ v5(진짜 LV 내부 + 파라메트릭 판·판막·건삭; 60만 면으로 감면 후 pymeshfix로 볼륨 복원) ∪ 관상동맥 튜브 (manifold). 4방 절개면 = 심첨–승모판 중심–삼첨판 중심(RV–RA 라벨 접촉) → 반절 A/B + `_PRINT_ORIENTED`. 다른 케이스는 v5 대신 hires LV 심근을 씀 | 1009: 속 빈 외피 440k면 164 mL 수밀; 합집합 **1,076,922면, 수밀, 재료 336 mL**(부스러기 0.08 mL 제거); 반절 152.4 + 183.6 = 336.0 ✓ 둘 다 수밀. 렌더 `whole_heart_hollow_render.png`(반절 A에 LV 육주·유두근·건삭, RV·심방 내강, 폐정맥·SVC·대동맥 단면). 한계: 삼첨판·폐동맥판 없음, 혈관 끝 막힘, IVC 없음, 우측 벽 두께 합성 |
| 26 | `build_whole_heart_anatomy.py` (`--stage lv\|hollow\|union\|cut\|recap\|anatomy\|render\|all`) | **리마스터 + 해부학적 부위 분할.** (1) AV-plane 판(12.5 mm, 34.9 mL — 단독 LV 모형에서 판막을 잡던 장치; 전심장 안에서는 LA·대동맥 뿌리 자리를 차지해 '원통'처럼 보였음) 제거 → LV 고체 = hires 심근 ∪ 승모판 ∪ 대동맥판 ∪ 건삭 (`BLENDER_OUT/LV_v6_noplate.stl`, 94만 면 수밀). (2) 25단계의 1 mm 스킨이 승모·대동맥 구멍을 막고 있던 것 수정(바깥과 접한 곳만 스킨). (3) 부위별 STL: RV/LA/RA/대동맥/PA/폐정맥/SVC 벽(합성 벽 복셀을 자란 내강 기준으로 귀속), LV 심근(정점 라벨: 중격 = RV 내강 6 mm 이내, 유두근 = CT 후보), 승모·대동맥·삼첨·폐동맥판, 건삭, 관상동맥 3계통(LAD계·LCx계·RCA계 튜브) → `whole_heart_hollow/parts/` + `parts_manifest.json`(색·출처·부피); 절개 반절에 부위 색 → `*_anatomy.ply` | 1009: 합집합 1,129,748면 수밀 단일 313.6 mL(판 제거로 −23 mL), 반절 134.8 + 178.7 ✓, 부품 16종. 렌더 `whole_heart_hollow_v2_anatomy_render.png` |
| 27 | `generate_right_valves.py` | **삼첨판·폐동맥판(파라메트릭).** 판륜 = 라벨 접촉면(RV–RA, RV–PA)의 중심·법선(PCA)·반경(90백분위): 삼첨 3첨판(전 140°·1.15 r, 중격 120°·0.9 r — LV 방향으로 정렬, 후 100°), 폐동맥 3 반월첨(0.9 r, 배 0.4 h); 두꺼운 격자 고체(1.0 mm), 부착 고리 1 mm 벽 속에 → `frame_A_patient/{tricuspid,pulmonary}_valve_parametric.stl`, `right_valves.json` | 1009: 삼첨 판륜 r 22.2 mm, 폐동맥 r 15.2 mm, 첨판 모두 수밀. **첨판 모양은 문헌 비율**(CT에 첨판이 안 보임), 우측 건삭·유두근 없음 |
| 28 | `ct_coronary_checklist.py` + `ct_coronary_seg.py` 통과 규칙 | **관상동맥 누락 점검.** (a) SCCT식 분절표(LM, LAD 근·중·원위, 대각지, 중격지, LCx 근·원위, OM, RI, RCA 근·중·원위, RV/예각지, 결절지, PDA, PLV) 존재/부재 + 우세형; (b) 나무에 안 들어간 밝은 관형 구조(V≥0.08, HU≥200, 나무에서 >3 mm, 모델된 대혈관 제외) 목록 — HU≥300이면 '놓쳤을 수 있는 동맥', 아니면 '정맥일 가능성'. seg 쪽 수정: 덩어리·정맥형 구간이라도 **그 너머에 동맥(HU≥330, r≤2 mm, 30복셀+)이 이어지면** 끊지 않고 1.75 mm로 얇혀 통과(좌심방이 접촉·동정맥 합류 구간) → `coronary_checklist.{json,md}` | 1009: LCx 줄기 30 → **110 mm**, OM 5개(135 mm) 회복, 우세형 '좌우세 의심(PDA 미확인)'. 예상된 부재: 중격지·결절지(해상도), PDA/PLV/원위 RCA(석회화 뒤 희미). 남은 후보 3개(HU 413–424, LA 부근 37–54 mm) — 좌심방이 조각 또는 원위 LCx 가지, 미확정 |
| 29 | `build_whole_heart_anatomy.py --stage recap` + 새 `render` | **절단면(4방 단면) 마감.** `slice_mesh_plane(cap=True)`은 단면 고리마다 귀자르기 부채꼴로 뚜껑을 덮는데, 그 삼각형들이 단면을 가로지르는 길쭉한 조각이라 (a) 한 조각이 여러 구조를 걸쳐 부위 색이 뒤섞이고 (b) 화면에서 줄무늬로 보였다. 이제 뚜껑을 버리고 **몸통의 열린 경계**(정확한 단면 윤곽)에서 제약 들로네 삼각분할로 다시 만든다(Triangle `-pq28a3YY`: 내부만 ≤3 mm², **어떤 분절에도 Steiner 점 금지** — `-Y` 하나로는 볼록껍질만 보호되고 방 윤곽은 내부 분절이라 쪼개져 구멍이 생긴다). 심근/내강 판정은 점-다각형 규칙 대신 **분절을 건널 때만 뒤집는 넘침 채우기**(단면 윤곽이 얇은 벽을 스치며 자기 자신과 닿는 곳에서 점-다각형은 정의 자체가 모호함), 남은 누수(자기접촉점 ~300개 모서리)는 pymeshfix로 봉합. 단면 윤곽은 `section_loops_{A,B}.npz`로 저장해 렌더에 검은 선으로 덮어 그린다. 렌더도 손봄: 후면 제거 엄격화(0.03), 절단면은 평면광 + 밝기 보정으로 '갈아낸 면'처럼, 비-절단면만 감면(단면은 원본 유지), 얼룩 제거(선폭 0.45), **세 번째 패널 = 절단면 단독**(방 이름표) | 1009: 뚜껑 7,190 조각 → 28,110 면(A) / 7,075 → 27,690(B), 부피 134.8·178.7 mL 그대로, 둘 다 수밀 ✓. `triangle` 패키지가 없으면 기존 뚜껑을 그대로 두고 나머지는 동일하게 동작 |
| 30 | `export_unreal.py` → `UNREAL/<case>/`, `UNREAL_IMPORT.md`, `UNREAL/import_assets.py` | **Unreal 교육 앱 에셋 팩(0단계).** 26·29단계 메시를 glTF로 내보내면서 Unreal이 필요로 하지만 메시가 안 들고 있는 것을 붙인다: **좌표계**(환자 RAS mm → glTF 오른손·Y-up·m, `X=-X_ras, Y=Z_ras, Z=Y_ras`, 행렬식 +1로 손방향 보존 — 좌우 반전은 육안으로 안 잡히므로 '정면에서 LV가 화면 오른쪽' 검사를 문서에 박아 둠), 부위 색을 **정점 색으로 구워서** 머티리얼 하나로 전체 채색, **출처 배지**(환자/파라메트릭/합성)를 매니페스트에 동봉, 4방 절단면의 원점·법선(실시간 clip plane 기본값 = 프린트 반절과 같은 평면), 관류 영역 오버레이 메시 | 1009: 부위 16종 152만 f + 전심장 113만 f + 반절 A/B + 관류 = 213 MB, 56초. 검증: LV 중심이 RV보다 +34 mm(반전 아님), 크기 79×82×94 mm 실물, LV 정점 색 3종(심근·중격·유두근) 보존. glb는 `.gitignore`(`*.glb`)로 커밋 제외 — 스크립트로 재생성 |
| 13 | `render_sections.py v2|v3|v4`, `render_check.py` | 검증 그림 (`BLENDER_OUT/hollow_v*_sections.png` 등) | 단면 그림은 **패널당 하나의 2D 프레임** 사용 (초기 그림의 결함 수정) |

## 산출물 → 용도

| 용도 | 파일 | 주의 |
|---|---|---|
| 혈액풀 단독 프린트 | `CT/lv_bloodpool_CT_smooth.stl` (진짜 혈액, 육주 음각) 또는 `PRINT/frame_A_patient/lv_surface.stl` (라벨 그대로, 유두근 포함 공동) | 팬텀 코어는 아직 라벨 버전 — 아래 참고 |
| 판막 달린 속 빈 심실 프린트 | `BLENDER_OUT/hollow_ventricle_v2_fixed_valves.stl` | 판막은 파라메트릭(환자 것 아님) |
| **전체 어셈블리 — 환자 내부 (권장, 2026-09-17부터 v5)** | `BLENDER_OUT/hollow_ventricle_v5_CT_hires.stl` (v4와 같은 조립, 부품만 연속 필드 등위면) | 벽·내막·육주·유두근 = 환자(CT 0.49 mm). 첨판·판·건삭 경로 = 파라메트릭. v4는 비교용으로 보관 |
| 전체 어셈블리 — 이상화 유두근 (구버전) | `BLENDER_OUT/hollow_ventricle_v3_IDEALISED_subvalvular.stl` | v4가 나오기 전 버전. 유두근·건삭 **이상화** — 비교용으로만 |
| 교육용 절개 모형 | `BLENDER_OUT/hollow_v5_cutaway_{A,B}_PRINT_ORIENTED.stl` (v4 절개도 보관) | 절단면 아래로 출력. 내벽에 육주·유두근이 보임 |
| 실리콘 유동 팬텀 | `PHANTOM/core_PRINT_ORIENTED.stl` (PVA) + `mould_box_PRINT_ORIENTED.stl` (PLA/PETG) | 절차는 `phantom_design.json → procedure` |
| CFD (OpenFOAM) | `PHANTOM/cfd/LV_phantom_m.stl` + 스니펫 | frame A 좌표(m); 기존 `lv_cfd_patient`와 프레임 다름 |
| SimVascular (svMultiPhysics / svZeroD) | `SV/fluid_phantom_cm.vtp`, `SV/fluid_cavity_cm.vtp`, `SV/solid_myocardium_cm.vtp` | cm 단위. FSI용 정합 메시는 별도(로드맵 2번 벽) |
| **관상동맥 (인쇄·교육·CABG/PCI 설명)** | `CT/coronary/coronary_tree_print_frameA.stl` (튜브, ≥Ø2 mm) 또는 `coronary_tree_frameA.stl` (분할면 그대로) | 환자 것(CT). 근위–중간부만 신뢰 (`coronary_cpr.png`, `coronary_xsec.png` 참조); 원위부·PDA 없음. 전심장 모형과 합칠 때는 심외막에 1 mm 정도 묻혀 있으므로 union 전에 0.5 mm 오프셋 권장 |
| **관류 영역 / 위험 심근량 (교육·연구)** | `CT/coronary/territories/territory_map.png`, `territory_table.md`, `LV_myocardium_territories.ply`(정점 색) | 보이는 나무 + 고랑 사전정보; 임상 판단용 아님. SPECT/CMR 관류와 대조 전까지는 방법론 시연 |
| **전심장 + 관상동맥 (교육·전시)** | **`CT/whole_heart_ext/whole_heart_with_coronaries.stl`**(폐정맥·SVC·PA 가지 포함, 24단계) / 캐스트 `CT/whole_heart_ext/parts/*.stl` (10종); 라벨만 쓴 구판은 `CT/whole_heart/` | RV·심방·대혈관 벽 두께는 합성(균일). 판막 없음. IVC 없음(동맥기), 대동맥궁은 FOV 밖. 출력용은 `PA_CAP_MM=45`로 다시 생성하면 폐문 가지가 짧아져 덜 부러짐 |
| **다른 환자 (N례)** | `CT/cases/<id>/{coronary,hires,territories,whole_heart}` | `CASE=<id>`로 어떤 스크립트든; 요약 `CT/cases/SUMMARY.md` |
| **속 빈 전심장 4방 절개 (교육 최종형, v2)** | `CT/whole_heart_hollow/whole_heart_hollow_v2_4ch_{A,B}_PRINT_ORIENTED.stl` (+ 통째 `whole_heart_hollow_v2.stl`); 부위별 색은 `*_anatomy.ply`, 부위별 출력은 `parts/` 16종 + `parts_manifest.json` | 26·29단계. 판 없음, 승모·대동맥 구멍 열림, 4판막 모두(승모·대동맥·삼첨·폐동맥 = 파라메트릭), 절단면 재삼각분할(수밀 ✓). 절단면 아래로 출력, 벽 2–3 mm(합성) |
| Fusion 360 / CAD | `CAD/frame_A_patient/*.stl`, `PHANTOM/*.stl` | 몰드 파트는 정확한 원기둥·상자라 BRep 변환 용이 |

## 남은 선택지 (2026-09-16 저녁 기준)
- **팬텀 코어를 CT 혈액풀로 교체**: `make_flow_phantom_mold.py`의 LV 경로를 `CT/lv_bloodpool_CT_smooth.stl`로 바꾸면 되지만, 1 mm 미만 육주 틈은 PVA 코어에서 부러지고 실리콘에 기포를 가둔다. 반경 1.5 mm closing으로 미세 틈만 메운 버전을 코어로 쓰는 것이 맞다(유두근 형상은 유지됨).
- 판막은 여전히 파라메트릭. CT에는 판막 첨판이 거의 안 보이므로(이완기 정지 영상) 여기서 더 나아가려면 4D CT나 문헌 템플릿이 필요하다.

## 1차(교육·연구용) 범위 — 2026-09-17 기준

끝: 위 1–30단계. 산출물 목록과 말할 문장은 `EDUCATION_PACK.md`. PC1에서 남은 1차 작업: `batch_cases.py`로 20례 실행 → `CT/cases/SUMMARY.md`; ASOCA(40례)/ImageCAS(1,000례) CCTA 공개 주석으로 관상동맥 분할 Dice·중심선 오차; 실물 출력.
**1차에서 하지 않는 것**(2차·로드맵): FSI·능동수축(`SIMVASCULAR_ROADMAP.md`), 결절 전기생리, CT-FFR, 판막 정교화(실측 시트 전), 관류 영상(SPECT/CMR) 대조 — 병원 파트너와 익명 CCTA+관류 쌍 확보 후.
